
public class State
{
    // Example definition; deplace with own definition
    public (int, int) position;

    public State() { }

    public State((int, int) _position)
    {
        position = _position;
    }
}